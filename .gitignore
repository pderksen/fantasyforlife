node_modules/
dist/
data/**/players-*.json
# output/ is committed — Cloudflare Pages serves it directly from the repo
.claude/
*.code-workspace
# Staging area for full-res photo originals. Never committed: they are downscaled into
# assets/photos/ and deleted. See docs/photos.md.
photos-inbox/
